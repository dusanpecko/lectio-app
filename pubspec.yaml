name: lectio_divina
description: Aplikácia Lectio Divina pre každodenné zamyslenia nad Božím slovom.

publish_to: "none" # Remove this line if you wish to publish to pub.dev
homepage: https://www.lectio.one
repository: https://github.com/dusanpecko/lectio-app.git
issue_tracker: https://github.com/dusanpecko/lectio-app/issues
documentation: https://github.com/dusanpecko/lectio-app/wiki

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 11.0.1+6000003

environment:
  sdk: ^3.8.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  intl: ^0.20.2

  # Ostatné balíčky necháme s konkrétnymi verziami
  cupertino_icons: ^1.0.8
  flutter_dotenv: ^6.0.1
  carousel_slider: ^5.1.1
  flutter_html: ^3.0.0-beta.2
  shared_preferences: ^2.2.3
  sign_in_with_apple: ^8.1.0
  crypto: ^3.0.6
  google_sign_in: ^7.2.0
  google_fonts: ^8.1.0
  easy_localization: ^3.0.7
  rxdart: ^0.28.0
  package_info_plus: ^10.2.0
  url_launcher: ^6.3.0
  logger: ^2.4.0
  image_picker: ^1.1.2
  image: ^4.2.0
  path_provider: ^2.1.4
  fluttermoji: ^1.0.2
  supabase_flutter: ^2.10.0
  just_audio: ^0.10.5
  just_audio_background: ^0.0.1-beta.17
  audio_session: ^0.2.4
  audio_service: ^0.18.18
  get_it: ^9.2.0
  timezone: ^0.11.1
  permission_handler: ^12.0.1
  device_info_plus: ^13.2.0
  app_links: ^7.2.1
  home_widget: ^0.9.3
  http: ^1.4.0
  share_plus: ^13.2.0
  flutter_svg: ^2.0.17
  flutter_launcher_icons: ^0.14.4
  firebase_core: ^4.0.0
  firebase_messaging: ^16.0.0
  flutter_secure_storage: ^10.0.0
  # Spovedné zrkadlo: PBKDF2 + AES-GCM (čisté Dart) a biometrické odomknutie
  cryptography: ^2.7.0
  local_auth: ^2.3.0
  flutter_local_notifications: ^22.0.1
  provider: ^6.1.2
  cached_network_image: ^3.4.1
  # Priamy prístup k disk cache, ktorú používa cached_network_image — pre
  # predcachovanie podcast coverov na offline použitie.
  flutter_cache_manager: ^3.4.1
  connectivity_plus: ^7.2.0
  video_player: ^2.9.3
  webview_flutter: ^4.13.1
  in_app_review: ^2.0.10
  wakelock_plus: ^1.2.8
  showcaseview: ^5.1.0
  flutter_markdown_plus: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.3

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true
  assets:
    - .env
    - assets/translations/
    - assets/images/
    - assets/icon/
    - assets/video/

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/icon.png"  # base iOS ikona
  min_sdk_android: 21
  
  # Android Adaptive Icons (Material Design)
  adaptive_icon_background: "#4A5C7E"
  adaptive_icon_foreground: "assets/icon/icon_foreground.png"
  adaptive_icon_monochrome: "assets/icon/icon_monochrome.png"
  
  # iOS Light/Dark/Tinted Icons (iOS 18+)
  image_path_ios: "assets/icon/icon.png"  # light mode - farebná ikona
  image_path_ios_dark_transparent: "assets/icon/icon_dark.png"  # dark mode - svetlejšie pozadie
  remove_alpha_ios: true
